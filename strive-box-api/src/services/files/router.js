import { Router } from "express";

const router = Router();

//1. GET

router.get("/", () => {
  try {
  } catch (error) {}
});

//2. POST

router.post("/", () => {
  try {
  } catch (error) {}
});

//3. GET SINGLE

router.get("/:id", () => {
  try {
  } catch (error) {}
});

//4. UPDATE

router.put("/:id", () => {
  try {
  } catch (error) {}
});

//5. DELETE SINGLE

router.get("/:id", () => {
  try {
  } catch (error) {}
});

export default router;
